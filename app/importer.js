// Imports laps to the user data directory and stores them in a nice format.
// Stuff is stored and json because lazy

var remote = require('electron').remote;
var fs = remote.require('fs');


// Dump JSON into the data dir
function StoreSession(traces){
    
}

// Load JSON from the data dir
function LoadSession(sessionFile){
    
}
